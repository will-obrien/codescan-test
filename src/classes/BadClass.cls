/** Bad class is an example of really bad code */
public class BadClass {
    final integer variableX = 1;

    /** BadClass constructor */
    public BadClass () {
        system.debug(variableX);
    }
}
